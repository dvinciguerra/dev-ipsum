# frozen_string_literal: true

RSpec.describe DevIpsum do
  it "has a version number" do
    expect(DevIpsum::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
